<link href="~/Content/rating/rating.css" media="all" rel="stylesheet" type="text/css" />
<script src="~/Scripts/rating/star-rating.min.js" type="text/javascript"></script>

@model List<FinalTest.Domain.Contracts.ViewModels.PostViewModel>


<div class="col-md-6">
    <h2>@Resources.Resource.MostRatedNews</h2>
    @*<table class="table">
        <thead>
            <tr>
                <th>@Resources.Resource.Title</th>
                <th>@Resources.Resource.Description</th>
                <th>@Resources.Resource.Author</th>
            </tr>
        </thead>
        <tbody>*@
    @for (int i = 0; (i < Model.Count) && (i < 5); i++)
    {
        <div>
            <div>
                <strong>
                    @Html.DisplayFor(modelItem => Model[i].Title)
                </strong>
                @Resources.Resource.By @Html.ActionLink(Model[i].AuthorName, "Display", "Profile", new { id = Model[i].AuthorId }, null)
            </div>
            <div>
                @Model[i].Description
            </div>
            <div>
                @Resources.Resource.Created: @Model[i].Created
                <input class="rating" min=1 max=5 step=1 data-size="sm" readonly="true" value="@Model[i].Rating">
            </div>
            <div>
                @Html.ActionLink(Resources.Resource.Read, "Display", "News", new { id = Model[i].Id }, null)
            </div>
        </div>
        <hr />


    }
    <div>
        @Html.ActionLink(Resources.Resource.ReadAll, "Index", "News", null,htmlAttributes: new { @class = "btn btn-success" })
    </div>

</div>
